import React, { Component } from 'react';

import './DeleteNode.scss';

const DeleteNode = props => {
    return (
        <>
            <a className="delete-node">DELETE NODE</a>
        </>
    );
};

export default DeleteNode;
